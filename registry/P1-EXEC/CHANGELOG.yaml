changelog:
  methodology_id: P1-EXEC
  current_version: "1.2"

  entries:
    - version: "M1-COUNCIL v1.3"
      date: "2026-03-19"
      type: method_evolution
      method: M1-COUNCIL
      changes:
        - "RFC-003: Ax-8 anti-capitulation clause integrity — uses declared predicates has_defend_clause and has_acknowledge_clause over CharacterCard via card_of function symbol"
        - "RFC-003: Removed vacuous Question quantifier from Ax-8 (F-R-3) and free card variable (F-R-4)"
        - "Added has_defend_clause, has_acknowledge_clause predicates to domain theory"
        - "initial_state_valid_claim.fresh updated: Ax-1 through Ax-8 vacuously true"
        - "initial_state_valid_claim.resume updated: Ax-8 verified via prior sigma_1 postcondition"
        - "terminal_state_valid_claim updated: Ax-7 and Ax-8 coverage added"
        - "G1 compilation note updated: 18 predicates, Ax-8 uses declared predicates"
      evidence:
        rfcs: [RFC-003]
        sessions: [SESSION-037]
        decisions: [D-087]
        theory_review_findings: [F-R-1, F-R-2, F-R-3, F-R-4]

    - version: "M1-COUNCIL v1.2"
      date: "2026-03-18"
      type: method_evolution
      method: M1-COUNCIL
      changes:
        - "RFC-001: sigma_0 precondition branches for fresh/resume paths (CMEM-PROTO integration)"
        - "RFC-001: initial_state_valid_claim covers both fresh and resume initial states"
        - "RFC-002: Ax-7 gains minimum-turns guard |Turns| >= 2 * |Questions|"
        - "RFC-002: 2*|Questions| threshold tagged as calibration parameter"
      evidence:
        rfcs: [RFC-001, RFC-002]
        sessions: [SESSION-032]
        theory_reviews: [TC-002, TC-005]
        decisions: [D-067, D-068]

    - version: "1.1"
      date: "2026-03-14"
      type: methodology
      changes:
        - "Added routing_would_repeat predicate and mu_3 (routing accuracy measure)"
        - "Added routing_feedback section with schema and calibration note"
        - "RP-1 projection updated to carry full M1-COUNCIL sigma_3 output"
        - "Ax-RETRO added (retrospective obligation)"

    - version: "M1-COUNCIL v1.1 fixes"
      date: "2026-03-14"
      type: method_patch
      method: M1-COUNCIL
      changes:
        - "Added diminishing_returns predicate + Ax-7 (Leader halt)"
        - "Position identity operationalized via Leader tagging (Ax-3)"
        - "Added 4 predicates: decision_issued_by, specific_question, precedes, sigma_2_terminates"
        - "Added Council sort (was referenced but undeclared)"
        - "Bound free variables in Ax-4, Ax-5, Ax-6, Ax-7, O_COUNCIL"
      evidence:
        gap_candidate: "GC-P1EXEC-001"
        retro: "review-P1-EXEC.yaml"
      evolution_id: "EVO-P1EXEC-001"

    - version: "M2-ORCH consistent(Integration) operationalized"
      date: "2026-03-14"
      type: method_patch
      method: M2-ORCH
      changes:
        - "Added 3-check operationalization for consistent(Integration) predicate"

    - version: "1.0"
      date: "2026-03-14"
      type: methodology
      changes:
        - "Initial compilation: 3 execution methods (M1-COUNCIL, M2-ORCH, M3-TMP)"
        - "Predicate operationalization for 5 driving predicates"
        - "4 retraction pairs declared and verified"
