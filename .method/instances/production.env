# Production instance profile
# Default bridge configuration — matches existing behavior.

# Instance identifier (available as process.env.INSTANCE_NAME in the bridge)
INSTANCE_NAME=production

# HTTP port for the bridge server
PORT=3456
