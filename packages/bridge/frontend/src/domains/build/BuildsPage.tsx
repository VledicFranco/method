/**
 * BuildsPage — Top-level /builds route.
 *
 * Stub for Wave 3 prep. C-4 implements the full 3-column layout
 * with BuildList, BuildDetail, and ConversationPanel.
 *
 * @see PRD 047 — Build Orchestrator §Dashboard Architecture
 */

export default function BuildsPage() {
  return (
    <div className="min-h-screen bg-void p-8">
      <h1 className="font-display text-xl text-txt mb-4">/build orchestrator</h1>
      <p className="text-txt-dim">Build dashboard loading — Wave 3 (C-4) implements this view.</p>
    </div>
  );
}
