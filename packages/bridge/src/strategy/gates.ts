// Re-exported from @method/core for backward compatibility
export type { GateType, GateConfig, GateContext, GateResult } from '@method/core';
export { getDefaultRetries, getDefaultTimeout, evaluateGateExpression, evaluateGate, buildRetryFeedback } from '@method/core';
