# sample-fca-polyglot

Mixed-language fixture: TypeScript + Scala + Python components in one project
root. Used by `polyglot-scan.test.ts` to validate multi-profile detection
against the real filesystem.
