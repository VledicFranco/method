{
  "name": "sample-cortex-cognitive-monitor",
  "version": "0.0.1",
  "private": true,
  "description": "PRD-068 Wave 1 — Monitor cognitive module as a Cortex tenant app. Wraps pacta's MonitorV2 behavior; emits method.cortex.workspace.anomaly + .confidence; resumable pact with cortical-workspace handshake.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@method/agent-runtime": "*",
    "@method/pacta": "*"
  }
}
