# H4 — i1 — Epoch Methodology Implementation

**Session:** sess_450c68cd1ecf
**Date:** 2026-03-01
**Methodology:** method-iteration
**Delta at close:** 1.0

## Summary

Implemented two new YAML methodologies:
- `server/src/methodologies/epoch-open.yaml` — 3 phases: Theme, Experiments, Commit
- `server/src/methodologies/epoch-close.yaml` — 3 phases: Experiment Outcomes, Retrospective, Forward

Both loaded via `method_reload` without errors (7 total methodologies).

## Key Design Decisions

1. **`experiment_names` + `measurable_criteria` as separate required arrays** (not just free-form `experiments` array): forces the agent to explicitly enumerate both the slug and the falsifiable criterion for each experiment separately, making it structurally impossible to skip the "how we'll know" step.

2. **`hypothesis_md_content` / `decision_md_content` produced inside the session output**: the session itself is the primary artifact. The content can be copied directly to disk. This eliminates prose writing outside the session.

## Validation

- TC1: `method_reload` returned both `epoch-open` and `epoch-close` in the loaded list.
- TC2: Full `epoch-open` session (`sess_9220a7d2640b`) — delta=1.0, zero soft_warnings, all invariants passed on first attempt. Produced `hypothesis_md_content` for E4.
- TC3: Full `epoch-close` session (`sess_9985deb2cfba`) — delta=1.0, zero soft_warnings, E3 retrospective produced as `decision_md_content`.

All 5 acceptance criteria satisfied.

## Decision

Stop. Both methodologies implemented and validated. E3 decision.md produced by TC3 epoch-close session.
