# H1 — i1 — Soft Invariant Copy Fix

**Session:** sess_4e2444f33719
**Date:** 2026-03-01
**Methodology:** method-iteration
**Delta at close:** 1.0

## Summary

One-line fix in `server/src/runtime/validator.ts` line 25:

```
Before: `"${fieldName}" is required but missing`
After:  `"${fieldName}" was not provided`
```

## Findings

- Change is correct and type-checks clean.
- The Phase 2 soft_warning during the session itself demonstrated the bug live:
  `methodology_name` (a soft/optional field) showed "is required but missing" —
  exactly the misleading language being fixed.
- Runtime validation (TC1) could not be confirmed against the live server because
  Docker was running the old compiled code. Rebuild required.

## Blocker

Infrastructure changes require `docker:up` to take effect. `method_reload` only
reloads YAMLs. This is a known workflow constraint, not a code problem.

## Decision

Stop. The fix is complete. Pending docker:up for runtime confirmation.
