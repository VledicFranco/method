# H1 Decision — Confirmed

Thematic area is the right coherence principle. Conjunctive dependency is opt-in.
The default is: experiments within an epoch are independent and resolve in any order.

The three-level vocabulary that follows from this:
- **Epoch** = a concern area (theme) + a set of experiments
- **Experiment** = a falsifiable hypothesis within the theme
- **Iteration** = a method-iteration session advancing an experiment

No changes to method-iteration.yaml needed from this experiment.
CLAUDE.md update captured in H4.
