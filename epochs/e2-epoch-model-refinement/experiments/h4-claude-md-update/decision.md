# H4 Decision — Confirmed

CLAUDE.md updated successfully. The three-level vocabulary (epoch / experiment /
iteration) is now the organizing frame. Epoch = concern area, experiment = falsifiable
hypothesis, iteration = method-iteration session.

Key framing changes that landed well:
- "Theme + experiments" is clearer than "hypothesis" at the epoch level
- Retrospective framing for decision.md removes the implied binary verdict
- Status via file presence table is concrete and unambiguous
- Promotion rule gives an explicit escape valve for scope creep
