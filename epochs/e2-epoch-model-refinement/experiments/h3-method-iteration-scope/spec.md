# H3 — method-iteration.yaml Scope

**Hypothesis:** method-iteration.yaml correctly sits at the experiment level —
one session per experiment. No changes to the methodology are needed. The epoch
level is directory convention + CLAUDE.md, not a methodology session.

**How we'll know:** We can run E2's experiments using method-iteration without
hitting a phase that doesn't map to experiment-level work.

**Chosen methodology:** method-iteration (assessment only)
