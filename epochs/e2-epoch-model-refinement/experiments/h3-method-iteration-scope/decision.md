# H3 Decision — Confirmed, minor wording fix noted

method-iteration.yaml operates correctly at the experiment level. No structural
changes needed.

One minor wording fix identified in Phase 0 guidance: "epoch_criteria_open" field
description should reference the experiment's own acceptance criteria, not the
epoch's. Carrying this as a known issue — too small for its own experiment, will
be patched alongside any future method-iteration.yaml changes.
