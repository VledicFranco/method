# H2 — Directory Structure

**Hypothesis:** Moving to `experiments/hN-{slug}/` as the unit of work gives the
right granularity. Each experiment has its own spec and iteration logs. The epoch
level holds the theme and the retrospective.

**How we'll know:** E2 itself uses this layout. If it feels natural to work in,
the hypothesis holds. This experiment is self-validating.

**Chosen methodology:** method-iteration (implicit — E2 is the test)
